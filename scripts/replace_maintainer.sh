#!/bin/bash
# NEEDS WORK!
# 1. replace package names
# 2. replace licenses
# 3. replace repo name
# 4. replace github username
#MAINTAINER=$1
#OLD_MAINTAINER=warting

#find . -type f -exec sed -i "s/${OLD_MAINTAINER}/${MAINTAINER}/g" {} +
#find . -type f -name "*.kt" -exec sed -i "s/${OLD_MAINTAINER}/${MAINTAINER}/g" {} +
